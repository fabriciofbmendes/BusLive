import { Dimensions, StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        position:"absolute",
        marginTop:"7%",
        width:"100%",
        backgroundColor:'red',
        textAlign:"center",
    }
});